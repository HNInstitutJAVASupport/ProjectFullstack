Step to dockerized fullStack app :

SpringBoot :
 -Create DockerFile in the root folder of spring project. (see file directly for content)

Angular :
 -Create DockerFile in the root folder of Angular project. (see file directly for content)

Docker-Compose file :
 -Create docker-compose.yml in root folder containing all projects (see file directly for content)